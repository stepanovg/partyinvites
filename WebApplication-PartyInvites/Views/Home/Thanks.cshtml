@model PartyInvites.Models.GuestResponse

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Thanks</title>
</head>
<body>
    @{ 
        try
        {
            WebMail.SmtpServer = "smtp.googlemail.com";
            WebMail.SmtpPort = 465;
            WebMail.EnableSsl = true;
            WebMail.UserName = "forbackup2011@gmail.com";
            WebMail.Password = "goga;backup";
            WebMail.From = "forbackup@gmail.com";
            WebMail.Send("stepanov.g@hotmail.com", "RSVP notification", Model.Name + " is " + ((Model.WillAttend ?? false) ? " " : "not") + "attending");
        }
        catch (Exception)
        {
            @:<b> Sorry - we couldn't send the email to confirm your RSVP.</b>
        }
    }
    <div> 
        <h1>Спасибо, @Model.Name!</h1>
        @if (Model.WillAttend == true)
        {
            @:Здорово что придете, напитки уже в холодильнике ;)
        }
        else
        {
            @:Жаль что вы не придетеб но спасибо что дали об этом знать.
        }
       
    </div>
</body>
</html>
